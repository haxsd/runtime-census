# ============================================================================
# 项目级工具链声明（兼容格式）
# ============================================================================
#
# 放在项目根目录，文件名 .tool-versions。
#
# 为什么保留这个格式：
#   它是 asdf 时代的既有约定，mise 也原生支持，而且已经被大量现存项目采用。
#   nvm、pyenv、rbenv 等工具各自也有读取它的插件。
#   比起各写各的 .nvmrc / .python-version，一个文件表达所有语言更不容易漏。
#
# 格式说明：每行 "工具名 版本号"，以 # 开头的行是注释。
# 工具名用 asdf 的标准命名（nodejs 而不是 node）。
#
# 与 mise.toml 的取舍：
#   .tool-versions  —— 只能表达版本，写不了环境变量和任务。适合只想锁版本的简单项目。
#   mise.toml       —— 能表达环境变量、任务、依赖钩子。功能更全，推荐新项目使用。
#   两者同时存在时 mise 都能读，但不要在两处写冲突的版本。

nodejs 22.23.2
python 3.12.10
java   temurin-21.0.5
