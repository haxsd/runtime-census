# 行尾符策略。
#
# .sh 必须固定为 LF：Windows 上 Git 默认会把文本文件转成 CRLF，而带 CRLF 的 shell
# 脚本在 bash 下执行时会报 "/usr/bin/env: 'bash\r': No such file or directory"，
# 看起来像脚本坏了。这个仓库同时发 .ps1 和 .sh，所以必须显式钉住。
* text=auto
*.sh text eol=lf
